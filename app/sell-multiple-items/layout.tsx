import type React from "react"
export default function SellMultipleItemsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <div className="sell-multiple-items-layout">{children}</div>
}
